# PNG/JPG 转圆角 ICO

这是一个不依赖 Python 或 ImageMagick 的 Windows 小工具。它会：

- 从 PNG、JPG、JPEG 中心裁剪出正方形；
- 以 8 倍超采样生成圆角透明边缘，减少桌面图标锯齿；
- 自动用图标四周的邻近颜色填充原图中的透明黑角，避免缩放后出现暗角；
- 写入 16、24、32、48、64、128、256 共 7 个 32 位 PNG 图层；
- 自动读取手机照片常见的 EXIF 方向信息。

## 最简单的用法

把图片文件拖到 `转换PNG-JPG到ICO.cmd` 上，生成的 `.ico` 会统一放在：

`C:\Users\Administrator\Desktop\ChatGPT\转换ICO，以及图标圆角\修改图标存放`

也可以双击 `.cmd`，在弹出的窗口中选择一张或多张图片。

> **只能拖到 `转换PNG-JPG到ICO.cmd` 上。**
> 不要拖到 `_engine` 里的 `.ps1` 上：Windows 不会把 `.ps1` 当程序运行，拖上去只会报错（“不是有效的 Win32 应用程序”），不会转换。

## 目录说明

| 文件 | 用途 |
| --- | --- |
| `转换PNG-JPG到ICO.cmd` | 唯一需要操作的文件，拖放、双击都用它 |
| `_engine\png-jpg-to-ico.ps1` | 真正干活的脚本，放在子文件夹里，不要动它 |
| `_engine\last-run.log` | 每次运行留下的记录，见下一节 |
| `修改图标存放` | 输出目录，生成的 `.ico` 都在这里 |

## 窗口一闪而过时看哪里

`.cmd` 现在**一定会停在最后**并提示“按任意键继续”，所以正常情况下报错信息不会跑掉。

如果窗口还是一闪就没，说明 `.cmd` 根本没被执行（例如被拖到了别的文件上）。这时看 `_engine\last-run.log`：

- 里面有 `launcher started` → `.cmd` 跑起来了，问题出在脚本本身；
- 文件不存在，或里面的时间不是这次运行的 → `.cmd` 没被执行。

## 命令行

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\_engine\png-jpg-to-ico.ps1 .\logo.png
```

可调参数：

```powershell
# 圆角程度：18%（默认）；50% 接近圆形；0 为直角
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\_engine\png-jpg-to-ico.ps1 .\logo.jpg -RadiusPercent 18

# 给图案留 4% 透明边距
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\_engine\png-jpg-to-ico.ps1 .\logo.png -PaddingPercent 4

# 如需临时改到其他输出目录，可用 -OutputDir 覆盖默认位置
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\_engine\png-jpg-to-ico.ps1 .\logo.png -OutputDir .\ico
```

单独运行 `.ps1` 时，默认输出目录仍然是工具根目录下的 `修改图标存放`。

## 关于暗角

以前生成的图标，圆角外面容易带一圈暗边：源图自己的圆角边缘是"透明黑"时，脚本会把紧贴边缘的那层黑当成背景色，于是整条圆角边被涂黑。现在脚本改了两处：

- **取背景色**改成从每个角的**内侧一块区域**取平均色，不再取紧贴边缘的那几个像素；
- **生成时**，靠近边缘又偏黑的像素会被改写成上面取到的背景色。

所以源图本身带黑边，出来的 `.ico` 也不会有暗角了。要注意的是：这套逻辑对**深色图标**同样安全 —— 主体要是本来就暗，取到的背景色也就是主体色，描边不会被改花。

已经生成的旧图如果还有暗角，见本目录下的 `_去暗角修复`：里面按 `原始图标`、`圆角图标`、`ico`、`修改图标存放` 分好了同样的文件名，是去暗角后的副本，**原文件一个都没动**，你核对没问题后自己覆盖回去即可。`_对比图` 里是深色／白色背景下的前后对比图（左=原样，右=修后）。

网易云音乐快捷方式仅作为视觉参考读取；脚本不会修改它，也不会执行快捷方式里的任何内容。
